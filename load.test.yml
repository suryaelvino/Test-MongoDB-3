config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 100
scenarios:
  - flow:
      - get:
          url: "/projects"
          # withQueryParams:
          #   page: 5
          #   limit: 100